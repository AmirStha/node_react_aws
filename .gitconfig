[user]
    # Use a custom email address
    email = amir@bottle.com.np

[credential]
    # Note the use of the --profile switch
    helper = !aws --profile learn codecommit credential-helper $@
    UseHttpPath = true
